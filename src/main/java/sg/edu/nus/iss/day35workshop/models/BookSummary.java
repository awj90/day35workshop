package sg.edu.nus.iss.day35workshop.models;

// A record is a special class that has no methods, and once created, attributes cannot be changed
public record BookSummary(Integer bookID, String title) {
}
